# 손절실패 에이전트 구축 명세 (Claude Code 핸드오프)

> 이 문서 하나로 엔진 구현 → 더미 테스트까지 끝낼 수 있게 정리한 명세다.
> 단계·임계값·정답지가 모두 명세로 박혀 있으니, 이 문서를 기준으로 구현하면 된다.

---

## 0. 지금 할 일 / 범위

- **목표:** "끝난 거래 하나를 받아 '손절을 못 하고 버틴 정도'를 0~1 점수로 매기는" 엔진을 만든다.
- **이번 작업 범위:** 판정 엔진(두뇌) + 더미 데이터로 정답지 검증까지. **외부 의존성 0**으로 짠다(pandas/numpy 정도만).
- **지금 하지 않는 것(나중):** 증권사 CSV 파서, 실데이터 연동, '에이전트 껍데기'. → 7번 참고.
- **데이터 현황:** 키움 REST API에서 3개월치 분봉을 팀원이 수집 중. 수집 완료 전이라 **지금은 더미 데이터로만** 엔진을 완성·검증한다. 실데이터가 들어올 자리(파서 인터페이스)만 비워둔다.

---

## 1. 컨텍스트

- 전체 시스템은 끝난 거래를 진단하는 **3개 에이전트**(진입/심리/손절실패)로 구성된다. 각 에이전트가 "이 손실에 내 문제가 기여한 정도"를 0~1로 내고, **가장 높은 점수가 그 거래의 대표 문제**로 뽑힌다. → 세 에이전트가 **같은 의미의 점수**를 내야 비교가 된다(이 전제만 지키면 된다).
- 이 문서는 그중 **손절실패 에이전트**만 다룬다.
- **한 줄 요약:** 손실을 끊었어야 할 선을 넘고도 계속 들고 있었는지, 얼마나 심하게 그랬는지를 점수로 매긴다.

### 큰 전제 — 일봉 backbone

- 분봉이 항상 있는 게 아니라서 **엔진의 기본 해상도는 일봉(daily)**이다.
- 모든 시간 측정은 **'며칠(영업일)' 단위**, 하루 안의 움직임은 그날 **저가(low)**로 보수적으로 근사한다.
- 분봉은 **있으면 정밀도를 올려주는 선택적 레이어**일 뿐, 엔진 성립 조건이 아니다. (분봉이 0개여도 엔진은 완결된다.) → 8번 참고.

---

## 2. 데이터 / 표준 스키마

증권사 양식이 제각각이라, **무엇이 들어오든 아래 두 표로 변환한 뒤** 엔진에 넣는다.

### transactions (거래내역)

| 필드 | 타입 | 설명 |
|---|---|---|
| trade_id | str | 주문 고유번호 |
| ticker | str | 종목코드 (예: "005930") |
| side | "BUY" \| "SELL" | |
| qty | int | 체결 수량 |
| price | float | 체결 단가(원) |
| executed_at | date | 체결일 (일봉이라 날짜까지만) |
| fee | float | 수수료+세금 (없으면 0) |

### ohlcv (일봉 시세)

| 필드 | 타입 | 설명 |
|---|---|---|
| ticker | str | |
| date | date | |
| open / high / low / close | float | **반드시 수정주가** |
| volume | int | |

> **수정주가 주의:** 보유기간 중 액면분할·배당락이 끼면 MAE·이탈일 계산이 망가진다. 실데이터 연동 시 수정주가 옵션을 반드시 켠다. 더미 단계에선 신경 쓸 것 없음.

---

## 3. 판정 로직 (엔진 두뇌) — 6단계

### 1단계 — 분석 단위

- **거래 한 사이클** = 한 종목을 사서(보유 0→증가) 전부 팔아서(다시 0) 끝난 묶음.
- **수익 난 거래는 건너뛴다** (점수 = 0).
- 아직 안 판(미청산) 종목도 제외.
- **예외:** -20%까지 빠졌다가 회복해 수익으로 판 거래는 점수 0이되 `lucky_hold` 플래그만 따로 남긴다. ("잘한 게 아니라 운이 좋았다"용.)

### 2단계 — 손절선 정하기 (핵심)

사용자가 손절선을 직접 안 정한 경우가 많아 시스템이 부여한다. **우선순위:**

1. 사용자가 입력한 손절률이 있으면 → 그걸 쓴다.
2. 없으면 → ATR 기준: `손절선 = -(k × 최근 14일 ATR ÷ 매수 평단)`
3. 그것도 안 되면 → **-5% 고정** 폴백.

> ATR을 먼저 쓰는 이유: -5% 고정선은 원래 하루 ±7% 움직이는 종목한텐 노이즈라 손절실패를 오판한다.
> 단, 사용자에게 보여줄 땐 **-5% 고정선 기준 결과도 같이** 계산해두면 직관적이다(둘 다 계산 권장).

### 3단계 — 일봉에서 두 값 뽑기

보유기간 일봉을 훑어서:

- **MAE (최대 손실):** 보유 중 각 날의 *저가* 기준, 평단 대비 손실이 가장 컸던 값.
- **이탈일(breach day):** 손실이 처음으로 손절선 밑으로 내려간 날.

> 물타기가 있으면 평단이 중간에 바뀌므로, 손익률은 **그 시점까지의 평단** 기준으로 계산해야 정확하다.

### 4단계 — 4가지 탐지 룰

거래마다 아래 넷을 본다. (괄호 = 일봉 기준 디폴트 경고값, 4번에서 조정 가능)

1. **손절선을 넘긴 했나?** — MAE가 손절선보다 깊으면 "이탈". 안 넘으면 손절실패로 보기 어렵다.
2. **넘고 며칠 더 들고 있었나 (지연)** — 이탈일~판 날 영업일 수. (2영업일↑ 경고 / 5영업일↑ 심각)
3. **넘고 손실이 얼마나 더 커졌나 (확대)** — 최종 손실% − 손절선%. (5%p↑ 또는 손절선의 2배↑면 심각)
4. **손실 중에 더 샀나 (물타기)** — 마이너스 상태 추가매수 횟수. (2회↑ 또는 추가매수량 ≥ 최초매수량이면 심각)

> 물타기 경계: **"이 거래 한 건 안에서 손실 중 추가매수"는 손절실패가 맡고**, "거래들 사이 반복·재진입 패턴"은 심리 에이전트가 맡는다.

### 5단계 — 점수로 합치기 (0~1)

네 신호를 각각 0~1로 환산해 **가중평균**:

| 신호 | 가중치 |
|---|---|
| 손실 확대 | 0.35 |
| 지연 | 0.30 |
| 물타기 | 0.20 |
| 손절선 대비 초과손실 | 0.15 |

**안전장치:**
- 수익 거래 → 점수 = 0
- 손절선 아예 미이탈 → 점수 ×0.3

> ⚠️ 이 가중치 4개는 **검증된 값이 아니라 prior(추정)**다. 코드에서 **반드시 설정값으로 분리**해 쉽게 바꿀 수 있게 한다. (나중에 라벨 거래로 로지스틱 회귀를 돌려 확정할 예정. → 8번)

### 6단계 — 출력

- **오케스트레이터용(구조화):** 판정 유형(`지연형`/`물타기형`/(선택)`투매형`/`해당없음`) + 최종 점수 + **신호 4개 값** + 플래그(`lucky_hold` 등).
- **사용자용(자연어 근거 문장):**
  > "3월 4일에 -6.5%로 손절 기준(-6.4%, 변동성 기준)을 넘겼지만 6영업일을 더 들고 있었고, 그 사이 2번 더 사들이면서 손실이 -17.3%까지 커졌습니다."
- **개인화 추천 규칙:**
  > "기준선 이탈 후 2영업일 넘게 보유하면 경고"

> 최종 점수 하나만 내보내지 말고 **신호 4개 값을 항상 같이 노출**한다. 가중치가 조금 틀려도 "지연이 강했나 물타기가 강했나"는 그대로 보여서 진단 설득력이 가중치에 덜 의존한다.

---

## 4. 설정값 / 임계값 (전부 config로 분리)

```
데이터 해상도: 일봉(daily)

손절선 우선순위: [사용자입력] → [ATR 변동성] → [고정 -5%]
  ATR 기간: 14일          # (A) Wilder 표준 — 그대로 OK
  ATR 배수(k): 2.0        # (A~B) 관행 범위 1.5~3 — 성향 따라 조정
  고정 손절률: -5%        # (B) 폴백용 — 정밀 불필요

경고 기준값(디폴트):
  지연: 경고 2영업일 / 심각 5영업일      # (B) 보정 권장
  확대: 5%p 이상 또는 손절선의 2배 이상  # (B) 비율(2배)을 메인으로
  물타기: 추가매수 2회 이상 또는 추가매수량 ≥ 최초매수량  # (B) 보정 권장

점수 가중치:
  확대 0.35 / 지연 0.30 / 물타기 0.20 / 초과손실 0.15   # (B) 가설 — 데이터로 확정 예정
  안전장치: 수익→0점, 미이탈→×0.3                       # (A) 정의상 당연

미사용(향후 분봉 확보 시 활성화):
  분 단위 지연 룰, 일중 정밀 MAE
```

**근거 강도 표기:** (A) = 업계 표준이라 그대로 써도 됨 / (B) = 논리적 추정, 데이터로 보정 대상.
→ **(A)는 디폴트로 박고, (B)는 전부 config로 빼서 한 곳에서 바꿀 수 있게 한다.**

---

## 5. 구현 명세 (의사코드)

### build_cycles — 사이클 묶기 (부분매수/매도/물타기 처리, 가중평균)

```
함수 build_cycles(transactions):
    cycles = []
    종목별 그룹화 후 executed_at 오름차순 정렬

    for 각 종목의 거래들:
        pos_qty  = 0          # 보유 수량
        avg_cost = 0          # 평단
        cur = None            # 진행 중 사이클

        for order in 거래들:
            if order.side == "BUY":
                if pos_qty == 0:                      # 새 사이클 시작
                    cur = 새 사이클()
                    cur.entry_ts = order.executed_at
                    cur.ticker   = order.ticker
                    cur.buys, cur.sells = [], []
                    cur.cost_checkpoints = []         # (날짜, 평단) → MAE용

                in_loss = (pos_qty > 0) and (order.price < avg_cost)   # 물타기 판정
                avg_cost = (avg_cost*pos_qty + order.price*order.qty) / (pos_qty + order.qty)
                pos_qty += order.qty
                cur.buys.append({ts, price, qty, in_loss})
                cur.cost_checkpoints.append((order.executed_at, avg_cost))

            else:  # SELL
                sell_qty = min(order.qty, pos_qty)
                cur.sells.append({ts, price, qty: sell_qty})
                pos_qty -= sell_qty                   # 부분매도는 평단 안 바뀜

                if pos_qty == 0:                      # 사이클 종료
                    cur.exit_ts = order.executed_at
                    total_buy_cost  = Σ(buy.price*buy.qty)   # 수수료 반영하려면 +fee
                    total_sell_proc = Σ(sell.price*sell.qty)
                    cur.avg_buy_price   = total_buy_cost / Σ(buy.qty)
                    cur.realized_return = (total_sell_proc - total_buy_cost)/total_buy_cost*100
                    cur.avg_down_count  = count(buy.in_loss == True)
                    cur.avg_down_qty    = Σ(buy.qty where in_loss) / 최초 buy.qty
                    cycles.append(cur); cur = None

        # 거래 끝났는데 pos_qty > 0 → 미청산, 제외

    return cycles
```

### attach_path_features — MAE·이탈일 (평단이 중간에 바뀌므로 별도)

```
함수 attach_path_features(cycle, ohlcv, stop_line_pct):
    보유구간 = ohlcv에서 cycle.entry_ts ~ exit_ts, ticker 일치 날들

    MAE = 0; breach_date = None
    for day in 보유구간(오름차순):
        avg_that_day = cost_checkpoints에서 day 시점 유효한 마지막 평단
        unreal = (day.low - avg_that_day)/avg_that_day*100    # 저가 기준 보수적
        MAE = min(MAE, unreal)
        if breach_date is None and unreal <= stop_line_pct:
            breach_date = day.date

    cycle.MAE_pct     = MAE
    cycle.breached    = (breach_date is not None)
    cycle.breach_date = breach_date
    cycle.delay_days  = breach_date ~ exit_ts 영업일 수 (둘 다 보유구간 날짜로 카운트)
    return cycle
```

→ 이 두 함수 결과를 **4단계 룰 + 5단계 점수 공식**에 그대로 넣으면 엔진 완성.

### 권장 모듈 구조 (참고)

```
schema.py           # transactions / ohlcv / cycle 데이터클래스
cycles.py           # build_cycles
path_features.py    # attach_path_features (+ ATR, 손절선 결정)
rules.py            # 4가지 탐지 룰
scoring.py          # 점수 합산 + 안전장치
report.py           # 판정 유형 + 근거 문장 + 추천 규칙
config.py           # 4번 설정값 전부 (B 항목은 여기서만 수정)
agent.py            # 위를 묶는 진입점 (입력: transactions+ohlcv → 출력: 6단계 결과)
tests/test_dummy.py # 6번 정답지
data/dummy.py       # 6번 더미 데이터
```

---

## 6. 더미 데이터 + 테스트 정답지

판정 로직의 모든 경우를 일부러 건드리는 5개 거래. (영업일: 주말 제외)

### transactions

```
# A: 지연형 (손절선 넘고 한참 버팀)
A1  950130  BUY   20  10000  2025-03-03
A2  950130  SELL  20   8800  2025-03-13

# B: 물타기형 (손실 중 계속 더 삼)
B1  950220  BUY   10  20000  2025-03-03
B2  950220  BUY   10  18000  2025-03-06
B3  950220  BUY   20  17000  2025-03-10
B4  950220  SELL  40  17500  2025-03-12

# C: 정상 손절 (선 닿자마자 끊음)
C1  950310  BUY   10   5000  2025-03-03
C2  950310  SELL  10   4650  2025-03-06

# D: 수익 거래 (제외)
D1  950440  BUY   10  30000  2025-03-03
D2  950440  SELL  10  33000  2025-03-10

# E: 운 좋은 홀딩 (-20%까지 갔다 회복해 익절)
E1  950550  BUY   10  15000  2025-03-03
E2  950550  SELL  10  15750  2025-03-13
```

### ohlcv — (low / close)만, MAE·이탈 계산용

```
950130:  03-03(9900/9950) 03-04(9600/9700) 03-05(9300/9350)
         03-06(9100/9150) 03-07(8900/8950) 03-10(8700/8750)
         03-11(8500/8600) 03-12(8600/8700) 03-13(8800/8800)
950220:  03-03(19500/19800) 03-04(19000/19100) 03-05(18500/18600)
         03-06(18000/18100) 03-07(17600/17700) 03-10(17000/17100)
         03-11(16500/16700) 03-12(17500/17500)
950310:  03-03(4950/4980) 03-04(4800/4850) 03-05(4680/4700) 03-06(4650/4650)
950440:  (계속 상승, 손절선 안 닿음) 03-10 close 33000
950550:  03-03(14800/14900) 03-05(13500/13700) 03-07(12000/12200)
         03-10(13500/13700) 03-13(15800/15750)
```

> 더미는 low/close만 줬다. open/high가 필요하면 close와 동일하게 채우거나 합리적으로 보간한다(엔진은 low만 쓴다).

### 기대 판정 (테스트 정답지)

| 거래 | 기대 판정 | 이유 |
|---|---|---|
| A | **지연형, 높은 점수** | 이탈 후 6영업일 버팀, 손실 -12%로 확대(2배) |
| B | **물타기형** | 손실 중 추가매수 2회, 수량 3배 |
| C | **거의 0점** | 선 닿고 1영업일 만에 끊음(정상) |
| D | **0점(제외)** | 수익 거래 |
| E | **0점 + `lucky_hold` 플래그** | MAE -20%였지만 익절 |

> ⚠️ **B가 핵심 함정.** 물타기로 평단을 낮춰 **최종 손실률은 -2.8%로 작아 보인다.** 그래서 '손실 확대' 룰엔 안 걸리지만 '물타기' 룰엔 강하게 걸려야 한다. → **% 손실만 보면 안 되고 MAE·추가매수 행동을 따로 봐야 한다**는 걸 B가 검증한다. 테스트에서 B가 물타기형으로 잡히는지 꼭 확인.

---

## 7. 구축 순서 / 상태

| 항목 | 상태 |
|---|---|
| 판정 엔진 (두뇌) | ✅ 지금 구현 |
| 더미로 정답지 검증 | ✅ 지금 구현 |
| 증권사 CSV 파서 | ⬜ 나중 (실데이터 붙일 때) |
| 일봉/분봉 연동·수정주가 보정 | ⬜ 나중 (키움 REST, 3개월 분봉 수집 중) |
| 에이전트 껍데기/인터페이스 | ⬜ 나중 |

**진행 순서:** `build_cycles → attach_path_features → 룰 → 점수 → report`를 더미로 짜고, **6번 정답지(A~E 5개)가 전부 통과할 때까지** 돌린다. 통과하면 이번 작업 끝.

---

## 8. 설계 결정 / 주의사항 (반드시 반영)

1. **외부 의존성 0.** 더미만으로 엔진 전 구간이 돌아가야 한다. 실데이터·API는 이번 범위 아님.
2. **가중치는 prior다.** 5단계 가중치 4개를 코드에 하드코딩하지 말고 `config.py`로 분리. 나중에 "손절실패가 명백한 거래 10~20개를 라벨링 → (확대,지연,물타기,초과손실) 4개를 feature로 로지스틱 회귀 → 계수를 가중치로" 보정할 예정이므로, **신호 4개 값을 점수와 함께 항상 반환**하도록 구조를 잡는다(나중에 회귀 입력으로 재사용).
3. **신호 4개 값 노출.** 최종 점수만 내보내지 말 것(6단계 참고).
4. **분봉은 선택적 enhancement 레이어.** 엔진 기본은 일봉. `attach_path_features`가 해당 거래 보유구간에 분봉이 있으면 **일중 정밀 MAE·분 단위 지연 룰**을 쓰고, 없으면 **일봉 저가로 폴백**하는 구조로 설계(손절선 우선순위와 같은 폴백 패턴). 지금은 폴백 경로만 구현하고, 분봉 경로는 자리만 비워둔다.
5. **수정주가 필수.** 실데이터 연동 시 반드시 수정주가. 더미 단계엔 무관.
6. **세 에이전트 점수 호환.** 출력 점수는 "이 손실에 이 문제가 기여한 정도(0~1)"라는 동일 의미여야 진입/심리 에이전트와 비교 가능.
